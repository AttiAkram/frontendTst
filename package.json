{
  "name": "frontendtst",
  "private": true,
  "description": "Workspace root: the deployed app lives in montaggiopro/ (the folder the Vercel project builds).",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "cd montaggiopro && npm run build"
  }
}
